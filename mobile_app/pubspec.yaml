name: Smart_Multi_Compartment_System
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0' # Updated SDK constraint for modern packages

dependencies:
  flutter:
    sdk: flutter

  mqtt_client: ^10.2.0
  google_fonts: ^6.1.0
  cupertino_icons: ^1.0.8
  flutter_local_notifications: ^17.1.2 
  
  # CRITICAL FIX: Add firebase_messaging
  
  
  # RECOMMENDED UPDATE: Use a recent, stable version for Firebase Core


dev_dependencies:
  flutter_launcher_icons: ^0.13.1
  flutter_test:
    sdk: flutter
    
  # RECOMMENDED UPDATE: Use standard lints for modern Flutter SDK
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

flutter_icons:
  android: true
  ios: true
  image_path: "assets/logo.png"